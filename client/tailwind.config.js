/**
 * This is for the project to work
 *
 * But in order for the VS Code intellisense extension to work
 * There's must be a config file in root folder
 */
const rootConfig = require('../tailwind.config')
module.exports = rootConfig
